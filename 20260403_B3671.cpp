#include <bits/stdc++.h>
using namespace std;
const int kL = 1e5;
int n, r[kL], a[kL];
int main() {
  cin >> n;
  for (int i = 0; i < n; i++) {
    cin >> r[i];
  }
  for (int i = 0; i < n; i++) {
    cin >> a[i];
  }
  return 0;
}