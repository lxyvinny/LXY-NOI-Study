#include <bits/stdc++.h>
using namespace std;

long long ans, n, q;

int main() {
  cin >> n >> q; // 输入初始值 n 和操作次数 q

  // 循环处理每一次操作
  for (; q > 0; q--) {
    int k;
    cin >> k; // 输入当前操作的位 k

    if ((n >> (k - 1)) & 1) {
      continue; // 如果第 k 位已经是 1，则不需要操作
    }

    // 计算需要补的最小值
    long long t1 = n & ((1LL << (k - 1)) - 1), t2 = (1LL << (k - 1)) - t1;
    // t1：n 的低 (k-1) 位
    // t2：补齐到第 k 位变成 1 所需增加的最小值

    n += t2;   // 更新 n
    ans += t2; // 累加答案
  }

  cout << ans; // 输出所有增加值的总和
  return 0;
}

/*
二进制位表示：
给定一个数字 n，我们需要对它进行操作，使得在二进制下的第 k 位为 1。
二进制从右到左编号，第 k 位对应的是 2^(k-1)。
操作过程：
对于每次操作，检查 n 在第 k 位是否已经是 1。如果已经是 1，什么也不做。
如果第 k 位是 0，那么我们需要加一个最小的值 x，使得 n 的第 k 位变为 1。这个值 x 等于 2^(k-1)，因为我们要确保仅修改第 k 位。
操作后影响：
每次修改 n 后，下一次操作会基于修改后的 n，因此需要记录每次 n 的变化。
解题步骤
初始化：
给定初始的 n 和操作次数 q。
处理每次操作：
对于每次操作给定的 k：
检查 n 的第 k 位（对应 n & (1 << (k-1))）。
如果该位是 0，那么需要通过加上 x = 2^(k-1) 来修改它，更新 n。
计算每次操作所需的 x 并将其累加。
输出结果：
输出所有操作需要的 x 的总和。
思考关键点
位运算：二进制的位操作非常重要。通过 n & (1 << (k-1)) 判断第 k 位是否为 1，如果是 0，则加上 2^(k-1)。
逐步更新：每次操作都会修改 n，所以要在每次操作后更新 n。
效率问题：由于 q 可能高达 10^5，必须保证每次操作的时间复杂度为 O(1)，所以总体时间复杂度应为 O(q)，这可以通过直接操作二进制位实现。

*/